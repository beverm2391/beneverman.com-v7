# Thread handoff — 2026-07-08

For the next Claude thread. Ingest, act, then delete this file (durable
knowledge lives in PROMPT.md / docs/renderer.md / docs/tasks/, not here).

## Read first

1. `PROMPT.md` — commands, deploy, routes, gotchas.
2. `docs/renderer.md` — conceptual model of the shadow/light pipeline.
3. `docs/tasks/001-ship-sun-production.md` — task currently being
   executed by Codex.

## Working model (agreed with Ben this session)

- Claude is the **staff engineer**: audit, spec, review, product judgment.
  No implementation grunt work — when code needs changing, write a
  numbered instruction file in `docs/tasks/` and Ben hands it to Codex.
- Priorities Ben named: communicate clearly, prefer proven community
  techniques (documented with references, not necessarily packages), and
  keep Ben's mental model intact — update PROMPT.md/renderer.md as things
  change.

## Current state

- Branch `codex/sun-position-icon`, ~35 commits ahead of `main`. Deploy is
  Vercel on merge to main — **do not merge until task 001 lands**; the
  config currently still has `mixed` mode with sanity-check ray values.
- **In flight**: Codex is executing task 001 (production default → `sun`
  mode, unmount shadow layer in sun mode, rays preset back to 0.5). It
  touches `src/App.tsx` and `src/siteVisualConfig.ts` on this branch —
  don't edit those files until it lands.
- Recent commits: `2d51b7d` light rays, `cecd657` ray-diffusion control +
  cranked preset, `6ebd680` process docs (PROMPT.md, renderer.md, task 001).
- `src/SunIconLab.tsx` / `.css` are another thread's untracked work —
  leave them.

## Next deliverable: audit → task 002 (scene cleanup spec)

Claude-side judgment work, read-only, can start before 001 lands (002
execution depends on 001). Audit `src/V2ShadowLayer.tsx` (~1130 lines),
`src/App.tsx` (~1585), and `lSystemShadowSource.ts`, `shadowFoliage.ts`,
`shadowSourcePreview.ts`, `debugTimeline.ts` for dead code. Then write
`docs/tasks/002-scene-cleanup.md`.

Direction already proposed to Ben (not vetoed):

- Delete `paper`, `blobs`, `branch` modes outright; keep `canopy`,
  `window`, `mixed`, `pool`, `curtain`, `sundial`, `sun` as promotion
  candidates.
- Extract scene builders from V2ShadowLayer into a `scenes/` module with a
  small `build(scene, settings)` / `animate(group, t, sunAngle)` interface;
  V2ShadowLayer becomes pure pipeline.
- Debug panel → per-scene param registry (each scene declares its relevant
  params), per-scene localStorage presets + copy-JSON button.
- Fix stale README (references nonexistent `src/experiments/`).

## Roadmap after 002

1. 001 lands → verify → PR branch to main → Vercel deploys sun-only prod.
2. Ben promotes simple scenes to production one at a time as they mature
   (pool / curtain / sundial are the candidates he liked).
3. Parked idea (Ben-acknowledged): max-blend caster resolve — invert the
   height encoding and use THREE.MaxEquation so overlapping casters union
   physically instead of alpha-stacking.

## Session-environment notes

- Detached dev server usually on port 5174.
- Headless captures this session used puppeteer-core + real Chrome
  (`--use-angle=metal`, fresh `--user-data-dir`, battery spoof — see
  PROMPT.md gotchas). The script lived in the old session's scratchpad and
  is gone; recreate from the PROMPT.md recipe if needed.
